spring:
  application:
    name: healthy_meal_backend
  config:
    import: classpath:application-secret.yml
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

springdoc:
  swagger-ui:
    path: swagger-ui

server:
  port: 4912

logging:
  level:
    root: info